<% if ( title ) { %>
<h2 id="<%- gennerateID( name ) %>"><%= title %></h2>
<% } %>
<% if ( description ) { %>
<div class="doc_desc">
    <%- markdown( description ) %>
</div>
<hr />
<% } %>

<% if ( items ) { %>
<a id="<%- gennerateID( name + ':' + name ) %>" ></a>
<%- renderTpl( 'items' ) %>
<% } %>

<% _.forEach( classes, function( clazz ) { %>
<%- clazz.isPlugin ? '' : renderTpl( 'class', clazz ) %>
<% }) %>